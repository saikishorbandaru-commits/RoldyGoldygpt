# Production HTTPS backend used by the packaged Android app.
# Required for AI bargaining, scrap appraisal and transparent try-on cutout APIs.
VITE_API_BASE_URL=https://api.your-roldygoldy-domain.com
