# Android module

This module is prepared for Capacitor 8. After `npm install`, run `npm run android:sync` so Capacitor generates/updates:

- `capacitor.settings.gradle`
- `app/capacitor.build.gradle`
- `app/src/main/assets/public`
- native plugin registrations

Open the `android` folder in Android Studio after sync.
